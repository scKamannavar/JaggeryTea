import React from 'react'

const NavbarElements = ({children}) => {
  return (
    <div className='text-sm font-Montser font-bold text-slate-700 cursor-pointer'>
      {children}
    </div>
  )
}

export default NavbarElements
